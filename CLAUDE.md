# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository Purpose

This is the special GitHub profile repository (`abdullahsherdy/abdullahsherdy`). Its only content is `README.md`, which is rendered on the owner's GitHub profile page. There is no application code, build system, or test suite.

## Working in This Repo

- All changes are edits to `README.md`. Since GitHub renders it, preserve valid HTML/Markdown mixing (the file uses inline HTML like `<p align="center">` alongside Markdown).
- The README relies on external badge/stat services: skillicons.dev, github-readme-stats.vercel.app, github-readme-streak-stats.herokuapp.com, and readme-typing-svg.herokuapp.com. Keep the `username=abdullahsherdy` query parameters intact when editing these URLs.
- Preview rendering matters more than code correctness — check that emoji, badges, and centered sections still render sensibly after edits.
